== README

This is Twitter API Rails Application with using PostgreSql and Rails '4.1.1'.
In this application :

* User can sign is with Twitter.

* User an schedule his/her tweet.

Steps to use application :

* Rake db:create

* Rake db:migrate
